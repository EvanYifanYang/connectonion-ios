# Project Instructions

- Do not run tests locally after code changes.
- Prefer local build/compile checks while iterating.
- Run local tests only when the user explicitly asks for a specific test/debug run.
- All tests, including unit tests and UI tests, should run after push in GitHub Actions via `.github/workflows/ios-tests.yml`.
